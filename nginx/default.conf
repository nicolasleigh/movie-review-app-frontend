upstream nginx {
  server nginx:5173;
}

upstream backend {
  server backend:8000;
}

server {
  listen 80;

  location / {
      proxy_pass http://nginx;
  }
  
  location /api {
      proxy_pass http://backend;
  }
}